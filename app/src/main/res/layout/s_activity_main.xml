<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:screenOrientation="portrait"
    android:background="#1ca4fb"
    tools:context="com.main.toledo.gymtrackr.StartActivity">

    <Button
        android:id="@+id/workoutNow"
        android:text="Workout"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        style="@style/myButton"
        android:layout_above="@+id/workoutFromPlan" />

    <Button
        android:id="@+id/workoutFromPlan"
        android:text="Open Plan"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        style="@style/myButton"
        android:layout_above="@+id/viewHistory" />

    <Button
        android:id="@+id/viewHistory"
        android:text="History"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        style="@style/myButton"
        android:layout_alignParentBottom="true" />

    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:src="@drawable/ic_logo"
        android:layout_above="@+id/workoutNow"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="46dp" />

 </RelativeLayout>
